from distutils.core import setup

setup(
    name='ActiveCampaign',
    version='0.0.1',
    author='mo',
    packages=['includes'],
    description='ActiveCampaign python API.',
    long_description=open('README.md').read()
)
